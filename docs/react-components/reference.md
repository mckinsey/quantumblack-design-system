# reference

- Look up three references for `{name}` before building the API
- **Library recipe** — nearest match in the host component library
- **Headless primitive** — docs for the underlying primitive
- **Sibling** — closest existing component in the host repo
- Return a short note per source: `library` / `primitive` / `sibling`
- Apply: structure from sibling, naming from library, behaviour from primitive
- Feed into step `api`
