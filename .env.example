VITE_BASE_PATH=""
QBDS_REGISTRY_URL="http://localhost:4123"

# Required by `npm run code-connect:*` to read/publish Code Connect mappings.
# Generate at https://www.figma.com/developers/api#access-tokens (needs the
# `code_connect:write` scope to publish).
FIGMA_ACCESS_TOKEN=""
