# build

- Implement `{component_path}` for `{name}`
- Inputs: alignment table + variant × state matrix (`spec`), reference notes (`reference`), approved prop table + part list (`api`)
- Use the host styling system and tokens
- Geometry from the variant × state matrix, not primitive defaults
- Do not add props beyond the approved API
- Output: component file at `{component_path}`
