# reference

QBDS binding for step `reference`. Run after [react-components/reference.md](../react-components/reference.md).

- **shadcn** — `npx shadcn@latest search {name}`, then `npx shadcn@latest docs {match}` or `npx shadcn@latest view {match}`
- **Base UI** — `https://base-ui.com/react/components/{name}`
- **Sibling** — closest match in `src/components/ui/`
